{
  "name": "college-bus-tracking-root",
  "version": "1.0.0",
  "description": "Root package for College Bus Tracking & Transport Management System",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "mongodb": "^7.5.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
